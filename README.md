# Teoría de Lenguaje 75.31 - Trabajo Práctico: Scala

## Ejecución del proyecto

Primero, iniciar el contenedor:

```bash
docker compose up -d
```

Luego, ejecutar el proyecto:

```bash
docker compose exec scala bash

# Dentro del contenedor
sbt run
```
